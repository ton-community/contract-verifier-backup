#include "../../node_modules/@layerzerolabs/layerzero-v2-ton/src/funC++/contractMain.fc";
#include "handler.fc";

;; The purpose of this file is to couple both the 'handlers' and the 'contractMain' together
;; This is the entry point of the contract...
;;
;; The 'contractMain' is the contract's main() function
;;      - main()
;; The 'handlers' are the functions that are called by the contract's main() function
;;      - executeActions()
;;      - executeOpcode()
;;      - checkPermissions()
