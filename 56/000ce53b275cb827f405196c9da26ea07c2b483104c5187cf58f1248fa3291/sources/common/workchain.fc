const WORKCHAIN = 0;

(int) is_same_workchain(slice address) inline {
  (int wc, _) = parse_std_addr(address);
  return wc == WORKCHAIN;
}

() check_same_workchain(slice address, int error_code) impure inline {
  throw_unless(error_code, is_same_workchain(address));
}
