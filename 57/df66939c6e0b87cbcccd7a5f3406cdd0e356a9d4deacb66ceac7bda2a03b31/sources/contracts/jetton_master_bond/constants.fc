const ONE_TON = 1000000000;
;; const master::v_ton = 1000 * ONE_TON; ;; Virtual TON liquidity for initial AMM operation (xy=k), set to 1000 TON.
;; const master::ton_the_moon = 1500 * ONE_TON; ;; Threshold for listing on DEX, triggers when ton_reserves reach 10000 TON.
const const::precision = 1000; ;; Denominator for calculate fee_rate and commission.
;; const master::fee_rate = 10; ;; Trading fee: fee_rate / precision = 1%.
const const::commission = 100; ;; Listing commission: commission / precision = 10%.
const const::price_precision = 1000000000; ;; Decimal precision for price calculations.
const const::airdrop_rate = 10; ;; Airdrop rate: airdrop_rate / precision = 1%.
const const::farm_rate = 10; ;; Farm rate: farm_rate / precision = 1%.
const slice TON_VAULT_ADDR = "EQDa4VOnTYlLvDJ0gZjNYm5PXfSmmtL6Vs6A_CZEtXCNICq_"a;