#include "../node_modules/@layerzerolabs/layerzero-v2-ton/src/funC++/classlib.fc";

const int Fee::NAME = "Fee"u;

const int Fee::bps = 0;

cell Fee::New(int bps) inline method_id {
    return cl::declare(
        Fee::NAME,
        unsafeTuple([
            [cl::t::uint16, bps] ;; Fee::bps
        ])
    );
}

const int Fee::_bpsOffset = _HEADER_WIDTH;

int Fee::getBps(cell $self) impure inline {
    return $self.cellPreloadUint16At(Fee::_bpsOffset);
}